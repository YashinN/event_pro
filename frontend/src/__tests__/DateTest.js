// gets api request function to test.
import { getCurrentDate } from "../utils/utlis";

test("should replace image link size 100x100 with 400x400", () => {
  expect(getCurrentDate()).toHaveLength(10);
});
